const PizzaPlace = "Pizza4U";
let numOfToppings = 10;
console.log (typeof PizzaPlace);
console.log (typeof numOfToppings);

console.log(`Welcome to ${PizzaPlace} we offer up too ${numOfToppings} toppings per Pizza!`);

if (numOfToppings<10) {
console.log("Quality, not quantity.");
}else {
  console.log("A whole lot of pizza");
}

{ for (let numOfToppings = 0; numOfToppings <= 10; numOfToppings += 2) { console. log(numOfToppings); } }
